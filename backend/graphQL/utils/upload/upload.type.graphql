scalar Upload

type File {
    _id: ID!
    filename: String!
    contentType: String!
    data: String!
}

type Query {
    retrieveFile(idFile: ID!): File!
}

type Mutation {
    singleUpload(file: Upload!): File!
}